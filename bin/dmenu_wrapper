#!/bin/bash

if hash rofi; then
    rofi -show run
else
    dmenu_run \
        -fn '-*-terminus-medium-r-normal-*-12-*-*-*-c-*-*-u' \
        -nb \#202020 \
        -nf \#f8f8f8 \
        -sb \#404040 \
        -sf \#ffffff
fi
